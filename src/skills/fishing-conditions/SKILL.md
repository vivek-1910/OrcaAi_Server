# Fishing conditions

Use the relevant API-backed weather, marine and official-alert tools for the
user's water mode and location. Report timestamps, units and missing sources.
Open-Meteo is the operational forecast fallback and is not a navigation
substitute. If an API source is unavailable, Scoutify may search the restricted
trusted-domain index for fresh secondary context, clearly labeled as research.
