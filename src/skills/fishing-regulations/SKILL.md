# Fishing restrictions

Only report restrictions returned by a configured API. Do not turn government
HTML pages, PDFs or search snippets into live legal facts. If no API is
configured, say that the restriction status is unverified.
